const discord = require('discord.js');
const logger = require('../logger.js');
const config = require('../config.js');
const shortid = require('shortid');
const xdate = require('x-date');
const client = new discord.Client();

let help = {};

function sleep(milliseconds) {
  var start = new Date().getTime();
  for (var i = 0; i < 1e7; i++) {
    if ((new Date().getTime() - start) > milliseconds){
      break;
    }
  }
}

// Data
var datem = new Date().format('dddd ddS  mmm, yy'); // Sunday 21st  Jan, 18
var date = new Date().format('dd/mm/yyyy'); // 21/01/2018

help.help = (message) => {
  try {
			let helpembed = new discord.RichEmbed()
      .setColor('#7B91E5')
      .setThumbnail(client.displayAvatarURL)
      .setTimestamp()
      .setTitle("db.rcpd.pl")
      .addField(`⚙ Potrzebujesz pomocy? Poniżej znajduje się spis komend:`, " • /report - komendą report możesz zgłosić, czego potrzebujesz.\n\`/report [nick ic] [zmiana postaci, nowy nick blah.. blah.. blah..]\`\n")
      .addField(`Dodatkowe komendy`, " • /get [id] - za pomocą tej komendy sprawdzisz stan reportu\n")
      message.author.send(helpembed);

      }  catch (e) {
        message.channel.send("error!");
        logger.log(e);
 }
}



module.exports = help;
